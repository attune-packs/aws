ref: aws.ec2_create_tags
label: "Tag EC2 Instances"
description: "Set tags on 1-100 explicitly identified EC2 instances"
enabled: true
runner_type: python
runtime_version: ">=3.10"
entry_point: aws_action.py
parameter_delivery: stdin
parameter_format: json
output_format: json
default_execution_permission_set_refs: [standard]
parameters:
  credential_key: {type: string, default: "aws.credentials", required: true}
  region: {type: string}
  instance_ids: {type: array, items: {type: string}, minItems: 1, maxItems: 100, required: true}
  tags: {type: object, required: true}
output:
  operation: {type: string, required: true}
  result: {type: object, required: true}
tags: [aws, ec2, tags, mutation]
