ref: aws.s3_list_objects
label: "List S3 Objects"
description: "List S3 objects with bounded SDK pagination"
enabled: true
runner_type: python
runtime_version: ">=3.10"
entry_point: aws_action.py
parameter_delivery: stdin
parameter_format: json
output_format: json
default_execution_permission_set_refs: [standard]
parameters:
  credential_key: {type: string, default: "aws.credentials", required: true}
  region: {type: string}
  bucket: {type: string, required: true}
  prefix: {type: string}
  max_results: {type: integer, default: 100, minimum: 1, maximum: 10000}
  page_size: {type: integer, default: 100, minimum: 1, maximum: 1000}
  starting_token: {type: string}
output:
  operation: {type: string, required: true}
  result: {type: object, required: true}
tags: [aws, s3, objects, discovery]
