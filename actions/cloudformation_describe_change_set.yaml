ref: aws.cloudformation_describe_change_set
label: "Describe CloudFormation Change Set"
description: "Describe one named CloudFormation change set"
enabled: true
runner_type: python
runtime_version: ">=3.10"
entry_point: aws_action.py
parameter_delivery: stdin
parameter_format: json
output_format: json
default_execution_permission_set_refs: [standard]
parameters:
  credential_key: {type: string, default: "aws.credentials", required: true}
  region: {type: string}
  stack_name: {type: string, required: true}
  change_set_name: {type: string, required: true}
  max_results: {type: integer, default: 100, minimum: 1, maximum: 1000}
  starting_token: {type: string}
output:
  operation: {type: string, required: true}
  result: {type: object, required: true}
tags: [aws, cloudformation, changeset, discovery]
